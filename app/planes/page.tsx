/**
 * La ruta /planes redirige desde el layout a /panel/tienda (si logueado con centro)
 * o a login (si no logueado). Esta página no se muestra en flujo normal.
 */
export default function PlanesRedirectPage() {
  return null;
}
